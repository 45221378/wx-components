<!--index.wxml-->
<view class="container">

	<block wx:for="{{circleList}}" wx:key="index" wx:for-item="items" wx:for-index="index">
		<view class="circle">
			<circle draw='{{items.id}}' per='{{items.precent}}' r='{{items.r}}' />
     
		</view>
	</block>
</view>